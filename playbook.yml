---
- hosts: all
  become: true
  tasks:
  - name: install yum packages
    yum: name={{ item }} state=latest
    with_items:
    - httpd
  - name: start httpd
    service: name=httpd enabled=yes state=started
